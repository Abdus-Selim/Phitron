#include <stdio.h>

int main()
{

    // Printing to ask for integer value

    int num;
    printf("Please enter an integer: ");
    scanf("%d", &num);

    printf("Here is the return : %d", num);

    return 0;
}